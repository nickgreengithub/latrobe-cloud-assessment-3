# Copy to .env for local development.
# SQLite file path. In Docker this is overridden to file:/data/rss.db (a volume).
DATABASE_URL="file:./dev.db"

# Empty means "same origin" — the frontend calls relative /api/... paths.
# Set this to point the RSS Client at a different server on camera.
NEXT_PUBLIC_API_BASE=""
